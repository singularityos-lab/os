[Unit]
Description=WPA supplicant (wifi backend for NetworkManager)
Before=network.target NetworkManager.service
Wants=network.target
After=dbus.service

[Service]
Type=simple
ExecStart=/usr/sbin/wpa_supplicant -u
Restart=on-failure
RestartSec=2s

[Install]
WantedBy=graphical.target
